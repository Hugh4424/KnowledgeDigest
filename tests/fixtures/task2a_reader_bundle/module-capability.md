The dataset-identification capability checks the user's query against schema fields before looking for matching values.[^src-16-dataset]

This hand-written fixture is only an attribution probe; it is not a semantic publication.
