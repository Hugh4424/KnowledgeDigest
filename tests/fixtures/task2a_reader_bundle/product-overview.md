GoInsight supports more than one deployment mode and can register as a DC in the controlled deployment mode.[^src-43-dc]

This hand-written fixture is only an attribution probe; it is not a semantic publication.
