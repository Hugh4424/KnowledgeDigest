# 历史执行证据

本目录保存已结束阶段的执行证据和 phase card。它们不是 KnowledgeDigest 的运行时输入，也不参与当前 `digest` 默认路径。

迁移前的旧相对路径是 `apply/evidence/` 和 `apply/phase-cards/`；当前可访问路径分别是本目录下的 `evidence/` 和 `phase-cards/`。证据文件正文中的原始绝对路径、运行 ID 和旧路径字符串属于历史事实，保留不改；当前计划、fixture 和索引已更新为新归档路径。
