Task 2-B T014 current final regression (2026-08-12)

Current code snapshot: `7cf8c40e6703c8284527d74b2cec8bf2d60119d8`.

The semantic-evidence precondition was not satisfied, so the frozen T014 command was not run against the tracked historical `apply/evidence/T013.semantic-run.json`. That old evidence was not reused as a current run.

Commands and results:

- `uv run --frozen pytest tests/acceptance/test_task2b_body_compiler.py -q -k 'not semantic_evidence_file'` — exit 0, 74 passed, 1 deselected.
- `uv run --frozen pytest tests/acceptance/test_task2_publication.py tests/acceptance/test_task2_batch_recovery.py tests/acceptance/test_task2_corpus_regression.py tests/acceptance/test_task2a_reader_bundle.py tests/acceptance/test_task2a_reader_frontmatter.py tests/acceptance/test_task2a_okf_smoke.py -q` — exit 0, 82 passed, 2 skipped.
- `uv run --frozen pytest -q` — exit 0, 514 passed, 3 skipped.
- WorkflowHub canonical build-code test capture for `uv run --frozen pytest -q` — exit 0; receipt `quality/tests/build-code-task2b-20260812-v1.json`; output hash is recorded by the receipt.

Limits:

- `semantic_evidence_file` was intentionally excluded from the focused command because there is no new T013 evidence file for this attempt.
- The T013 real provider run did not start: `KD_LLM_API_KEY`, `KD_LLM_MODEL`, and `KD_LLM_BASE_URL` are absent in the current invocation, and the frozen new-output path is occupied by a tracked historical evidence file.
- These green deterministic tests do not close the Task 2-B semantic exit. Delivery remains `not_released`.

## Current bound T013 semantic evidence (2026-08-12)

The user-authorized real semantic rerun was executed with the frozen 20-example / 30-source sample, qwen3.6, and environment-only credentials. The run completed and wrote a new evidence file; provider credentials were not written to repository files.

- run_id: `run-9076fc7b8e8440bb96c03f801bf636d8`
- evidence: `apply/evidence/T013.semantic-run-current-20260812.json`
- evidence sha256: `a47f51cc653f82f1a023683993e480f8cd11d083ca1adf787c3e9f16fc0ea05b`
- run_status: `completed`; execution_mode: `real_semantic`; delivery_status: `not_released`
- provider: qwen3.6 at the approved environment endpoint; provider calls: `14/14`
- semantic result: `1` machine-passing concept; page types: `module_or_capability` only; evidence backtrace: `168`; complete sections: `1`
- validator result: `valid=false`, `machine_exit_passed=false`, `reader_eligible=false`
- validator reasons: machine-passing concept count is below 6; `product_overview` coverage is missing; `procedure_or_rule` coverage is missing.

This current T013 evidence is intentionally not treated as a passing semantic gate. The deterministic regression commands above remain green, but Task 2-B remains `incomplete/not_released` and the frozen T014 exit is not satisfied.

After recording the current T013 result, the current worktree regression was rerun: `uv run --frozen pytest -q && git diff --check` — exit 0, `514 passed, 3 skipped`; `git diff --check` passed. This is a current deterministic regression fact only; it does not change the semantic gate result.

## Root-cause audit after the user-authorized continuation

- The frozen Task1 TopicIndex contains 54 rows, with 31 `published` and 23 `degraded`; it contains no `page_type`/`digest_page_type` field and no registered Task0 machine fixture exists for the missing types.
- The current Task2-B adapter is intentionally fail-closed: it maps only the existing product-overview/module branches and requires an explicit page type for `procedure_or_rule`; it does not infer page type from titles or prose.
- Therefore the current frozen input cannot legally produce the required `product_overview` and `procedure_or_rule` coverage. Reusing Task2A/Task2B behavior fixtures or changing the compiler to guess would violate the current decision/spec/plan.
- A separate typed-provider audit found no safe local change that turns the current semantic run green: unsupported claim/body pairings are correctly rejected by the existing faithfulness gate. Relaxing that gate would publish unverified reader facts.

Result: Task 2-B remains `incomplete/not_released`; this is an upstream evidence/registration handoff, not a reason to mark the current run passed.
