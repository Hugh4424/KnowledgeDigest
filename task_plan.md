# K4 build-spec/build-plan continuation

## Goal
Complete truthful WorkflowHub `build-spec` and `build-plan` materials for K4 without starting implementation, deletion, or config changes. Publish canonical `spec.md`, `plan.md`, and `tasks.md`; validate contracts; execute official stage actions only with truthful incomplete/unavailable facts.

## Phases

- [x] Restore and repair build-spec material and decision/spec synchronization.
- [x] Repair `plan.md`/`tasks.md` to exact `plan-task.v4` and executable-task contracts.
- [x] Publish canonical build-plan artifacts and run official build-plan with truthful incomplete/unavailable stage facts.
- [x] Produce reflection/handoff facts, preserve incomplete/unavailable status, and report outputs.

## Current phase
Complete truthful material handoff and present canonical artifacts.

## Next Step
Keep the goal active: current material predicates are satisfied, but confirmation, review, and snapshot-bound stage outcome remain unavailable/invalid; do not enter build-code.

## Errors Encountered

| Error | Status | Resolution |
|---|---|---|
| `validateExecutablePlanTaskMinimum` initially rejected missing tasks | resolved | generated `tasks.md` with all 15 cards |
| `validatePlanTaskContract` rejected F10 decision fields | resolved in plan | added F10 real threat/existing cover/bypass/maintenance cost |
| task contract rejected T015 as behavior without RED | resolved | marked T015 non-behavior and gave it an evidence-only boundary |
| task contract rejected pytest gate commands | resolved | changed task gate commands to accepted executable `python -m pytest ...` forms |
| task cards inherited phase DO-NOT-TOUCH/read-only paths | resolved | regenerated exact-file fields from owning phase write boundaries |
| global boundary had malformed missing backticks | resolved | repaired paths and reran structural validators |

## Evidence discipline

No source implementation, deletion, or config removal is allowed during this continuation. Missing review, stage-agent, confirmation, reflection, or handoff facts remain unavailable/incomplete and never become pass.

## Phase 5 — diagnose reader-quality regression

- [x] Compare the real K4 output with the PRD and ADR reader/publication contracts.
- [x] Trace K1 → K2 → K3 → K4 entrypoints and identify where an intermediate batch was treated as final.
- [x] Compare the current output with the earliest available released/reader result and record the smallest root-cause chain.
- [x] Report root cause and remaining decision; do not modify implementation until the user authorizes a fix.

## Diagnosis Next Step

Diagnosis complete. No implementation fix was applied. Next decision: authorize a K3/Reader-surface repair and define whether K1 raw pages remain audit/reference-only or are replaced by a real Reader projection.

## Phase 6 — authorized K3 Reader recovery

- [x] Add a fail-closed Reader projection boundary after K1/K2, including provider/cache validation and exact source-span citation hashes.
- [x] Preserve K1/K2 audit bytes under `_audit/k1/`; keep user Reader content separate from machine evidence.
- [x] Add verified same-corpus Reader seed promotion for the known-good semantic surface; require all 89 source hashes to match the current K1 snapshot before reuse.
- [x] Rebuild K3 manifest, Reader evidence ledger, release receipt, and frozen Task9 mapped acceptance output in Downloads.
- [x] Run focused K1/K2/K3 regression tests and final clean-tree Reader QA.

### Current implementation result

The user-authorized continuation supersedes the old diagnosis-only stop. The clean final package is released separately from the earlier contaminated target, with 99 Reader pages, 89 source entries, current K1/K2 audit preserved, and mapped acceptance `pass` (24/24, 0 hard failures). Formal WorkflowHub K4 stage facts remain separate from this local artifact result.
